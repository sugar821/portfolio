
/div.col-md-6.col-md-offset-3
div.col-12
  = form_for @task do |f|
      - if @task.errors.any?
        #error_explanation
          h2 = "#{pluralize(@task.errors.count, "error")} prohibited this task from being saved:"
          ul
            - @task.errors.full_messages.each do |message|
              li = message
      .field
        = f.label :title
        = f.text_field :title, class: "form-control",placeholder: "たいとる"
      .field
        = f.label :hours
        = f.number_field :hours, min:1,max:24,class: "form-control"
      .field
        = f.label :complete
        </br>
        = f.check_box :complete
      .field
        =f.label :category_id
        =f.collection_select :category_id, Category.all, :id, :name , { include_blank: t(".include_blank") },  { class: "form-control" }
      .field
        =f.label :image
        |<br>
        =f.file_field :image
      .actions = f.submit nil , class: "form-control btn btn-primary mt-2"
